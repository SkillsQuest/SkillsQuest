{
  "name": "带条件的成就声明齐全就是干净的",
  "why": "成就的 when 走 cond/expr、读的属性走 attr/self，两条都声明就不该报错。",
  "doc": {
    "spec": "skilltree/1",
    "meta": { "name": "成就" },
    "layout": "flow",
    "nodes": [{ "id": "a", "title": "A", "x": 0, "y": 0 }],
    "edges": [],
    "attrs": [{ "key": "str", "name": "力量", "type": "number" }],
    "awards": [
      { "id": "aw", "name": "力者", "need": 1, "when": { "gte": [{ "get": "attr.str" }, 10] } }
    ],
    "requires": ["cond/expr", "attr/self"]
  },
  "expect": { "ok": true, "issues": [] }
}

{
  "name": "有 maxRank>1 却没声明 rank/alloc 是 error（§3.6 双向绑定）",
  "why": "老客户端不认识 maxRank，会把加点节点当成布尔节点 —— 点一次就再也点不动，而那正是加点这类树的全部意义。",
  "doc": {
    "spec": "skilltree/1",
    "meta": { "name": "天赋" },
    "layout": "flow",
    "nodes": [{ "id": "a", "title": "A", "x": 0, "y": 0, "maxRank": 3 }],
    "edges": []
  },
  "expect": {
    "ok": false,
    "issues": [
      { "level": "error", "code": "CAPABILITY_NOT_DECLARED", "path": "/requires",
        "detail": { "capability": "rank/alloc", "gate": "nodes[].maxRank" } }
    ]
  }
}
